import styled from 'styled-components';

const Head = styled.thead`
  border-spacing: 0px;
  border-bottom: 1px solid rgba(22, 29, 37, 0.05);
  & > tr > th {
    font-weight: 600;
  }
`;

export default Head;